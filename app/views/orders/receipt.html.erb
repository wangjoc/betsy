<h1 class='title'>RECEIPT <%= @order.id %></h1>
<div class='page-container'>
  <h1 class='title'>Hope you regret your purchase!</h1>
  <div class="jumbotron">
    <h1 class="display-4">Your Order Details (Order #<%= @order.id %>)</h1>
    <p class="lead">Enjoying your regrets:</p>
    <hr class="my-4">
<%# TODO Leah will update this view %>
<%= render partial: "order_details", locals: { action_name: "New" } %>
<%= button_to "Return to Browsing", products_path, method: :get, class: "btn btn-info" %>
</div>

